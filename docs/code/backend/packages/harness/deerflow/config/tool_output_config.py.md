# `backend/packages/harness/deerflow/config/tool_output_config.py`

> 代码事实分析（自动生成，基于 AST 静态分析，非 docstring 转载）  
> 源文件: `backend/packages/harness/deerflow/config/tool_output_config.py`  ·  行数: 63

**模块文档首行**（仅供参考）: Configuration for tool output budget protection.

## 模块概览
- 函数 0 个，类 1 个，模块级常量 0 个

## 依赖（import）
- `__future__` -> annotations
- `pydantic` -> BaseModel, Field

## 类
### 类 `ToolOutputConfig`  L8
- 继承: BaseModel
- _文档首行_: Config section for tool-result output budget enforcement.
- 类/实例变量:
  - `enabled` = Field(default=True, description='Enable the tool output b...
  - `externalize_min_chars` = Field(default=12000, ge=0, description='Character thresho...
  - `preview_head_chars` = Field(default=2000, ge=0, description='Sampling budget re...
  - `preview_tail_chars` = Field(default=1000, ge=0, description='Sampling budget re...
  - `fallback_max_chars` = Field(default=30000, ge=0, description='Maximum character...
  - `fallback_head_chars` = Field(default=8000, ge=0, description='Head characters fo...
  - `fallback_tail_chars` = Field(default=3000, ge=0, description='Tail characters fo...
  - `storage_subdir` = Field(default='.tool-results', description='Subdirectory ...
  - `exempt_tools` = Field(default_factory=lambda: ['read_file', 'read_file_to...
  - `tool_overrides` = Field(default_factory=dict, description='Per-tool externa...
