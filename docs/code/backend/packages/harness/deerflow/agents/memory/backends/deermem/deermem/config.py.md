# `backend/packages/harness/deerflow/agents/memory/backends/deermem/deermem/config.py`

> 代码事实分析（自动生成，基于 AST 静态分析，非 docstring 转载）  
> 源文件: `backend/packages/harness/deerflow/agents/memory/backends/deermem/deermem/config.py`  ·  行数: 256

**模块文档首行**（仅供参考）: DeerMem backend configuration (parsed from ``MemoryConfig.backend_config``).

## 模块概览
- 函数 0 个，类 2 个，模块级常量 1 个

## 依赖（import）
- 模块: logging
- `__future__` -> annotations
- `typing` -> Any, Literal
- `pydantic` -> BaseModel, Field

## 模块级常量
- `logger` = logging.getLogger(__name__)

## 类
### 类 `DeerMemModelConfig`  L27
- 继承: BaseModel
- _文档首行_: DeerMem's memory-update LLM config (langchain ``init_chat_model`` params).
- 类/实例变量:
  - `provider` = Field(default=None, description="langchain model_provider...
  - `model` = Field(default=None, description='Model name. None = no LL...
  - `api_key` = Field(default=None, description="API key (or rely on the ...
  - `base_url` = Field(default=None, description='Override base URL (e.g. ...
  - `temperature` = Field(default=None, description='Sampling temperature.')

### 类 `DeerMemConfig`  L43
- 继承: BaseModel
- _文档首行_: DeerMem-private configuration (self-contained, host-agnostic).
- 类/实例变量:
  - `storage_path` = Field(default='', description='DeerMem data root. Empty =...
  - `storage_class` = Field(default='', description='Dotted class path for an a...
  - `debounce_seconds` = Field(default=30, ge=1, le=300, description='Seconds to w...
  - `max_facts` = Field(default=100, ge=10, le=500, description='Maximum nu...
  - `fact_confidence_threshold` = Field(default=0.7, ge=0.0, le=1.0, description='Minimum c...
  - `max_injection_tokens` = Field(default=2000, ge=100, le=8000, description='Maximum...
  - `token_counting` = Field(default='tiktoken', description="Token counting str...
  - `guaranteed_categories` = Field(default_factory=lambda: ['correction'], description...
  - `guaranteed_token_budget` = Field(default=500, ge=50, le=2000, description='Token cei...
  - `staleness_review_enabled` = Field(default=True, description='Enable staleness review ...
  - `staleness_age_days` = Field(default=90, ge=30, le=365, description='Facts older...
  - `staleness_min_candidates` = Field(default=3, ge=1, le=50, description='Minimum stale ...
  - `staleness_max_removals_per_cycle` = Field(default=10, ge=1, le=50, description='Maximum facts...
  - `staleness_protected_categories` = Field(default_factory=lambda: ['correction'], description...
  - `staleness_max_lifetime_multiplier` = Field(default=20.0, ge=1.0, le=100.0, description="Creati...
  - `staleness_max_extension_days` = Field(default=3650, ge=90, le=36500, description='Absolut...
  - `consolidation_enabled` = Field(default=False, description='Enable memory consolida...
  - `consolidation_min_facts` = Field(default=8, ge=3, le=30, description='Minimum number...
  - `consolidation_max_groups_per_cycle` = Field(default=3, ge=1, le=10, description='Maximum number...
  - `consolidation_max_sources` = Field(default=8, ge=2, le=20, description='Maximum number...
  - `model` = Field(default_factory=DeerMemModelConfig, description='Me...
  - `tracing_callback` = Field(default=None, description='Optional observability c...
  - `should_keep_hidden_message` = Field(default=None, description='Optional ``hook(addition...
  - `host_llm` = Field(default=None, description='Host-injected pre-built ...
  - `trace_context_manager` = Field(default=None, description='Host-injected context-ma...
- 方法:
  #### `cls` `from_backend_config(cls, backend_config: dict[str, Any] | None) -> DeerMemConfig`    @classmethod  L232
    - _文档首行_（仅供参考）: Parse a ``backend_config`` dict.
    - 分支数 2，函数体节点数 108；return: cls(), cls(**known)
    - 调用: cls, items, sorted, warning

## 文件内调用关系
_无文件内调用_
